Act as a Senior Production Software Engineer.

Goal:
Remove the remaining Replit-specific Stripe connector dependency from Elevate360BrandHub so production on Render uses native Stripe SDK only.

Context:
Render deployment is working. PostgreSQL, OpenAI, DeepSeek, Resend, Drizzle, automation jobs, and AI router are healthy. The only remaining health failure is:

[stripe] init error: X-Replit-Token not found for repl/depl
"stripe":{"ok":false,"detail":"connector unavailable"}

This means the app still depends on stripe-replit-sync or Replit-specific Stripe connector logic, which cannot work on Render.

Do the following carefully:

1. Search the codebase for all Replit Stripe references:
   - stripe-replit-sync
   - X-Replit-Token
   - repl/depl
   - replit stripe
   - stripe connector
   - connector unavailable

2. Replace all Replit Stripe connector initialization with native Stripe SDK.

3. Create or update this file:

server/stripeClient.ts

Use this structure:

import Stripe from "stripe";

let stripeClient: Stripe | null = null;

export function getStripeClient(): Stripe | null {
  const secretKey = process.env.STRIPE_SECRET_KEY;

  if (!secretKey) {
    console.warn("[stripe] STRIPE_SECRET_KEY not configured");
    return null;
  }

  if (!stripeClient) {
    stripeClient = new Stripe(secretKey, {
      apiVersion: "2024-12-18.acacia",
    });
  }

  return stripeClient;
}

export function isStripeConfigured(): boolean {
  return Boolean(process.env.STRIPE_SECRET_KEY);
}

4. Update every file that imports or initializes Replit Stripe connector to instead import:

import { getStripeClient, isStripeConfigured } from "./stripeClient";

or correct relative path.

5. Update health check logic so Stripe health only checks native Stripe configuration.

Expected health logic:

const stripeOk = isStripeConfigured();

return:
{
  ok: stripeOk,
  detail: stripeOk ? "configured" : "STRIPE_SECRET_KEY missing"
}

Do NOT call Replit connector health.
Do NOT require X-Replit-Token.
Do NOT require Replit deployment context.

6. Update checkout/session/payment code to use:

const stripe = getStripeClient();
if (!stripe) {
  throw new Error("Stripe is not configured");
}

Then native Stripe calls like:
stripe.checkout.sessions.create(...)
stripe.webhooks.constructEvent(...)

7. Ensure webhook route verifies signatures using:

const sig = req.headers["stripe-signature"];
const event = stripe.webhooks.constructEvent(rawBody, sig, process.env.STRIPE_WEBHOOK_SECRET!);

8. Preserve existing routes and database behavior.
Do not break:
- /api/chat
- /api/health
- automation jobs
- DeepSeek routing
- OpenAI premium routing
- Resend
- Drizzle
- checkout success flow
- consultation_orders inserts

9. After code changes, run:

npm run check
npm run build

10. If successful, give me:
- files changed
- removed Replit Stripe references
- health check expected result
- final commit summary

Expected final /api/health after deployment:

{
  "status": "healthy",
  "checks": {
    "database": {"ok": true},
    "openai": {"ok": true},
    "ai": {"ok": true},
    "resend": {"ok": true},
    "stripe": {"ok": true, "detail": "configured"}
  }
}

Important:
Do not log Stripe secret keys.
Do not log webhook secrets.
Do not log payment data.
Do not expose customer PII.